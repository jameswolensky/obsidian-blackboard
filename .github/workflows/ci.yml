name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v5
        with:
          node-version-file: ".nvmrc"
          cache: "npm"
      - run: npm ci
      - name: TypeScript check
        run: npm run typecheck
      - name: Review lint (eslint-plugin-obsidianmd)
        run: npm run lint
      - name: No !important in styles
        run: "! grep -q '!important' styles.css"
      - name: Build
        run: npm run build
      - name: Unit tests
        run: npm test
